/**
 * App icons via lucide-react — semantic aliases so call sites stay stable.
 * Prefer adding a Lucide mapping here over new inline SVGs.
 */
export {
  Gamepad2 as IconLibrary,
  Download as IconDownloads,
  RefreshCw as IconSync,
  Settings as IconSettings,
  Search as IconSearch,
  Check as IconCheck,
  Square as IconSelectEmpty,
  SquareMinus as IconSelectPartial,
  SquareCheckBig as IconSelectAll,
  X as IconClose,
  TriangleAlert as IconWarn,
  Database as IconDatabase,
  Clock as IconClock,
  CircleCheck as IconCircleCheck,
  HardDrive as IconHardDrive,
  Loader2 as IconLoader,
  ExternalLink as IconExternalLink,
  RefreshCw as IconRefresh,
  Eye as IconEye,
  EyeOff as IconEyeOff,
  Minus as IconMinimize,
  Square as IconMaximize,
  Copy as IconRestore,
  LayoutGrid as IconLayoutGrid,
  List as IconLayoutList,
} from "lucide-react";
